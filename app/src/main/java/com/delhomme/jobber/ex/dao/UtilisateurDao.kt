package com.delhomme.jobber.ex.dao

/*
import androidx.room.Dao
import androidx.room.Insert
import androidx.room.Query
import com.delhomme.jobber.dataclass.Utilisateur

@Dao
interface UtilisateurDao {
    @Insert
    fun insererUtilisateur(utilisateur: Utilisateur): Long

    @Query("SELECT * FROM utilisateurs WHERE email = :email AND motDePasse = :motDePasse")
    fun trouverUtilisateurParIdentifiants(email: String, motDePasse: String): Utilisateur?
}
*/