package com.delhomme.jobber.ex.dataclass

/*

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "evenements")
data class Evenement(
    @PrimaryKey(autoGenerate = true)
    val id: Int = 0,
    val candidatureId: Int,
    val relanceId: Int,
    val appelId: Int,
    val entretienId: Int,
    val date: String,
    val corbeille: Boolean,
    val date_suppression: String,
    val utilisateurId: Int,
)
*/