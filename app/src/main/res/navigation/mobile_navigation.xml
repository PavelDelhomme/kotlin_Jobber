<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_appel_list">
    <activity
        android:id="@+id/nav_appel_list"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_contacts" />
    <activity
        android:id="@+id/nav_candidatures"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_candidatures" />
    <activity
        android:id="@+id/nav_contacts"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_contacts" />
    <activity
        android:id="@+id/nav_entreprises"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_entreprises" />
    <activity
        android:id="@+id/nav_entretiens"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_entretiens" />
    <activity
        android:id="@+id/nav_dashboard"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_dashboard" />
    <activity
        android:id="@+id/nav_calendrier"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_calendrier" />

</navigation>